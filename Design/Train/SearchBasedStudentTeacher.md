> **Status: Not currently considered.** Self-distillation from the base net plus
> search is distinct from the current external KataGo-teacher distillation path.

Student(base net) learns to approximate the result and statistics of the teacher(base net + search).
